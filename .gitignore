node_modules/
dist/
# Exception: the CI stub seeder ships a prebuilt dist (Dockerfile.ci does a plain
# COPY with no build step), so its dist must be tracked despite the dist/ rule.
!ci/stub-seeder/dist/
data/
*.log
.env
.env.local

# Temp scripts and debugging artifacts
*.tgz
*.diff
test*.mjs
verify*.mjs
migrate*.ts
migrate*.log
prisma_push.log
local-scripts/

# claude code (personal)
/.claude/settings.local.json
