nodeLinker: hoisted

# These deps need their build/postinstall scripts to run for the engine to work
# (native bindings + Prisma engine downloads). All from well-known maintainers.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  better-sqlite3: true
  esbuild: true
  prisma: true
